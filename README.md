# 🛡️ NeuroForge Nuxt

### The "Senior Engineer" External Brain for AI-Native Development.

**NeuroForge Nuxt** is a cognitive engineering system designed to transform AI agents from "slop generators" into disciplined Senior Full-Stack Engineers. It enforces an **Analysis-First** workflow by externalizing an agent's reasoning into a persistent memory layer (`neuroforge/`), ensuring production-ready architecture for Nuxt 4, Nitro, Prisma, and TypeScript.

---

## ⚡ The NeuroForge Impact

- 🚀 **10x Development Velocity**: Front-load complex architectural decisions to prevent hours of debugging later.
- 🧼 **90% "AI Slop" Reduction**: Eliminate hallucinated patterns, god-components, and hydration mismatches through rigorous pre-coding analysis.
- 📉 **Low Token Usage**: Dense, high-signal markdown files replace massive chat histories, keeping your context window lean and cost-effective.

---

## 📦 Installation

Bring the NeuroForge brain into your project instantly:

```bash
npx skills add CayOpoku/neuroforge-nuxt
```

---

## 🔥 The Workflow: Analysis Before Code

NeuroForge isn't a boilerplate; it's a **protocol**. Before writing a single line of code, the AI is forced to:

1.  **Scan**: Analyze the existing codebase, layers, and patterns.
2.  **Document**: Create micro-memory files in `neuroforge/` (e.g., `01-project-analysis.md`, `02-architecture-decisions.md`).
3.  **Reason**: Apply **10+ years of Senior SaaS standards** (Single Responsibility, DRY, Type Safety).
4.  **Wait**: Present the plan for human review before proceeding.

---

## 🕹️ Sample Prompts (Test Cases)

Use these prompts to trigger the NeuroForge protocol in your project:

### 1. The Instant Architectural Audit

> "I've just inherited this project (or started a new one). Activate NeuroForge analysis and give me a senior-level audit of the folder structure and potential spaghetti-code risks."

### 2. The Type-Safety Deep Dive

> "We're seeing too many `any` casts. Run a NeuroForge type audit, identify the root causes of our `npx nuxi typecheck` errors, and propose a `.types.ts` strategy."

### 3. The Feature Brainstorm (Analysis Mode)

> "I want to add a Multi-Tenant billing system using Paystack and Nuxt Layers. Don't write code yet. Use NeuroForge to map out the architecture, Prisma schema changes, and service layer logic first."

### 4. The Component Refactor

> "This 500-line Vue component is a mess. Perform a NeuroForge analysis on it. Break it down into feature-sliced components and composables according to Senior SaaS standards."

---

## 🛠️ Enforced Senior Standards

NeuroForge ensures your AI agent follows these Nuxt 4 / Nitro patterns religiously:

- **Type-Safe Everything**: Mandatory `.types.ts` files for logic > 5 lines. No inline god-interfaces.
- **Data Fetching Excellence**: Strict usage of `createUseFetch` for API clients and `useAsyncData` to prevent SSR double-fetching.
- **Hydration Safety**: Automatic detection of `window`/`document` usage and non-deterministic state causes.
- **Prisma Singleton**: Ensures database connections are managed correctly for HMR and production.
- **Nitro Skeletons**: Clean, typed event handlers with standardized error responses.

---

## 🔍 Validation Protocol

NeuroForge includes a built-in "Quality Gate." After every task, the agent provides a **Code Quality Verdict (e.g., 9/10)**, justifying the score against senior SaaS standards.

- **Run a Type Audit anytime**: `npx nuxi typecheck`
- **Verify SSR Consistency**: Tagged logging for `[SERVER]` and `[CLIENT]` contexts.

---

**Built for developers who care about architecture as much as speed.**
